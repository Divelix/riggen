"""Type stubs for ``riggen._riggen``, the extension module (crates/riggen-py).

Mirrors everything the Rust module exposes; ``pyright`` reads this, not the
``.so``. Grows with the SDK (plans/python-sdk).
"""

__version__: str
