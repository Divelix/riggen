[package]
name = "riggen-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# `glam` (through the re-export) and, from step 4 on, `TriMesh` for the
# content-hash check in `file::load`. No geometry is stored in the document.
riggen-mesh.workspace = true
serde.workspace = true
serde_json.workspace = true
