//! The crates.io name reservation for **riggen**, the blazingly fast,
//! lightweight robot assembler for RL researchers.
//!
//! This crate is deliberately empty. The application — a native GPU window
//! that assembles STL/OBJ meshes into a kinematic tree with sim-ready
//! inertials and collision geometry and exports MJCF and URDF — ships here
//! as `riggen` 0.1.0 (`cargo install riggen`). Until then, see
//! <https://github.com/Divelix/riggen>.
